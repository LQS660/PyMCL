# PyMCL requirements
# NOTE: keep this file ASCII-only! Old pip on Chinese Windows (GBK) fails to parse UTF-8 comments.
requests>=2.31.0
keyring>=25.0.0
# signs the textures property served by mclauncher/skinserver.py (offline custom skins)
cryptography>=42.0.0
PySide6>=6.6.0
PySide6-Fluent-Widgets>=1.5.0
