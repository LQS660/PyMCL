# PyMCL 启动器 — 后端对接说明

面向后端开发者。**UI 与后端解耦，对接时只改 `app/backend.py`。**

当前 `*_impl` 与 `search_*` / `get_*` 均为内存模拟，用来联调界面。换成真实下载、扫描、启动后，页面代码不用动。

---

## 1. 架构

```
app/pages/*.py          只调 BackendAPI 公开方法，不访问网络/磁盘
        │
        ▼
app/backend.py          ★ 唯一需要改的文件
        │  start_task() 把耗时操作丢进 QThread
        ▼
progress / log / finished / task_added   Qt 信号（自动回主线程刷新 UI）
        │
        ▼
启动页进度条与日志、下载任务卡片、右上角完成通知
```

规则：UI 不做 IO，后端不碰控件。通信只走方法调用 + 信号。

---

## 2. 任务回调协议

后台实现函数签名固定：

```python
def _xxx_impl(self, progress, log, *你的参数):
    progress(current: int, total: int, message: str)
    log(text: str)
```

| 回调 | 约定 |
|------|------|
| `progress(current, total, message)` | 驱动进度条。`total=0` 为不确定进度。`message` 显示在进度条下，如 `52.3/168.0 MB · 12.4 MB/s` |
| `log(text)` | 写入启动页日志框 |

取消：用户点取消后，下一次 `progress()` 抛 `TaskCancelled`。循环里定期调 `progress()` 即可，不必自己查标志。长阶段（解压）也要穿插 `progress(i, n, "解压中")`。

未捕获异常会变成任务失败通知。

发起任务：

```python
self.start_task("安装游戏 1.20.1", self._install_game_impl, version, loader, loader_version)
# 返回 task_id: str
```

---

## 3. 异步任务（带进度条）

| 公开方法 | 实现函数 | UI 入口 |
|----------|----------|---------|
| `install_game(version, loader="无", loader_version="")` | `_install_game_impl` | 原版游戏页「安装」 |
| `install_modpack(name, source="Modrinth")` | `_install_modpack_impl` | 整合包「安装」/ 导入文件。目录页也会尝试 `extra=` |
| `install_mod(name, instance="default")` | `_install_mod_impl` | Mod「安装」/ 链接 / 本地 jar |
| `install_shader(name, instance="default", extra=None)` | `_install_shader_impl` | 光影包 |
| `install_resourcepack(name, instance="default", extra=None)` | `_install_resourcepack_impl` | 资源包 |
| `install_datapack(name, instance="default", extra=None)` | `_install_datapack_impl` | 数据包 |
| `download_java(major)` | `_download_java_impl` | Java 页磁贴 |
| `launch_game(instance, version, account, username, memory_mb, width, height)` | `_launch_game_impl` | 启动页「启动游戏」 |
| `cancel_task(task_id)` | — | 任务卡片取消 / 启动页停止 |

目录页（Mod / 整合包 / 数据包 / 资源包 / 光影包）安装时优先：

```python
install_xxx(name, instance, extra=extra)
```

`extra` 为字典，至少含：

| 键 | 含义 |
|----|------|
| `name` | 显示名 |
| `instance` | 目标实例 |
| `source` | `全部` / `Modrinth` / `CurseForge` |
| `game_version` | 筛选框版本，如 `全部 (也可自行输入)` / `1.20.1` |
| `url` / `path` | 从链接或本地文件安装时有 |
| `author` `downloads` `tags` `description` `updated` | 搜索结果原字段，可按需使用 |

若函数不接受 `extra=`，UI 会降级为 `fn(name, instance)` 再降级 `fn(name)`。真实后端建议保留 `extra=`。

---

## 4. 同步查询

慢查询（联网搜索）建议日后也改成 `start_task`，完成后让页面 `reload`。当前 UI 按同步返回值立刻刷新。

### 4.1 版本 / 实例 / Java

| 方法 | 返回 | 真实实现建议 |
|------|------|----------------|
| `get_version_list()` | `[{version, type, date}]` | `type` 为 `release` / `snapshot` 等。Mojang piston-meta 或 BMCLAPI |
| `get_installed_versions(instance)` | `["1.20.1", ...]` | 扫描该实例 `versions/` |
| `get_instances()` | `[{name, versions, mc}]` | `versions` 为数量，`mc` 为当前版本字符串 |
| `create_instance(name)` | — | 建目录 |
| `delete_instance(name)` | — | 删目录 |
| `rename_instance(name, new_name)` | — | 改名 |
| `open_instance_folder(name)` | — | `os.startfile` / `xdg-open` |
| `uninstall_version(version, instance="default")` | — | 卸载版本 |
| `get_java_list()` | `[{name, major, path}]` | 注册表 + 常见路径 |

### 4.2 目录搜索

| 方法 | 入口 |
|------|------|
| `search_mods(query, source)` | Mod |
| `search_modpacks(query, source)` | 整合包 |
| `search_shaders(query, source)` | 光影包 |
| `search_resourcepacks(query, source)` | 资源包 |
| `search_datapacks(query, source)` | 数据包 |

`query` 为名称框文本。`source` 为 `全部` / `Modrinth` / `CurseForge`。

每条结果建议字段（缺省也能显示）：

```python
{
    "name": "Fresh Animations",
    "author": "FreshSX",
    "downloads": 95890000,          # 整数，UI 会格式化成「万/亿」
    "description": "一句话简介",
    "tags": ["16x", "动态效果"],    # 标题旁灰标签，最多显示 4 个
    "game_version": "1.12+",        # 或 version
    "updated": "4个月前",           # 或 date
    "source": "CurseForge",         # 或 Modrinth
}
```

可选：`get_installed_mods/shaders/resourcepacks/datapacks(instance)`、`delete_mod/shader/...`。目录页目前主要走搜索+安装；已装列表若要做，按同名约定补即可。

---

## 5. 信号

`BackendAPI` 已有：

| 信号 | 参数 | UI 用途 |
|------|------|---------|
| `task_added` | `task_id, title` | 任务页加卡片 |
| `progress` | `task_id, current, total, message` | 进度条 |
| `log` | `task_id, text` | 启动页日志 |
| `finished` | `task_id, success, message` | 完成/失败通知；`message=="已取消"` 不弹错误 |

可选（本包 UI 未强依赖）：`ui_changed`、`task_count_changed(count)`。

`task_title(task_id)` 供通知栏取标题。

---

## 6. 流式下载参考

```python
import time
import requests

def _real_download(self, progress, log, url: str, dest: str, label: str):
    done = 0
    start = time.time()
    with requests.get(url, stream=True, timeout=30) as r:
        r.raise_for_status()
        total = int(r.headers.get("Content-Length", 0))
        log(f"开始下载: {label}")
        with open(dest, "wb") as f:
            for chunk in r.iter_content(1024 * 256):
                f.write(chunk)
                done += len(chunk)
                speed = done / max(time.time() - start, 1e-6) / 1024 / 1024
                progress(done, total,
                         f"{done/1024/1024:.1f}/{total/1024/1024:.1f} MB · {speed:.1f} MB/s")
    log(f"下载完成: {label}")
```

多文件（libraries / assets）每完成一个调：`progress(已完成数, 总数, f"补全依赖库 ({n}/{m})")`。

---

## 7. 设置

保存时 UI 只调：

```python
backend.update_settings(settings: dict)
value = backend.get_setting(key, default)
```

后端自行落盘（`config.json` 等），启动时预填 `_settings`。标注「重启后生效」的项由后端决定提示或延迟应用。

| 键 | 类型 | 默认 | 谁消费 |
|----|------|------|--------|
| `ui_fly_animation` | bool | True | UI 飞入动画 |
| `ui_fly_duration_ms` | int | 620 | UI |
| `ui_scale_percent` | int | 100 | 后端（重启） |
| `ui_mica_effect` | bool | True | 后端（重启） |
| `share_libraries` / `share_assets` | bool | False | 后端隔离策略 |
| `download_threads` | int | 8 | 后端下载 |
| `default_memory_mb` | int | 4096 | 后端 / 新实例 |
| `default_resolution` | `[w, h]` | `[854, 480]` | 后端 |
| `ms_client_id` | str | `""` | 微软登录 |
| `curseforge_api_key` | str | `""` | CurseForge 搜索兜底 |

新增设置：设置页加控件 → `collect()` 加键 → `update_settings` 会收到。

---

## 8. 飞入动画

```python
self.window().fly_to_tasks(source_widget, text="模组名", color="#2E9B6B")
```

落点为侧栏「下载任务」。开关/时长读设置项。无 `fly_to_tasks` 时目录页会跳过动画直接安装。

---

## 9. 侧栏与页面（只读，不必改）

```
启动
实例
下载
  原版游戏 / Mod / 整合包 / 数据包 / 资源包 / 光影包 / 下载任务
设置
Java
```

顶栏白底，选中项绿色点缀。主题色 `setThemeColor("#2E9B6B")`。

---

## 10. 运行

```bash
pip install -r requirements.txt
python main.py
```

Windows 也可双击 `run.bat`。

若 `pip install PySide6` 与 Addons 文件冲突：

```bash
pip install PySide6-Essentials
pip install --no-deps PySide6-Fluent-Widgets
```

---

## 11. 目录

```
pymcl_launcher/
├── main.py
├── run.bat
├── requirements.txt
├── README.md
├── 对接说明.md          ← 本文
└── app/
    ├── backend.py       ← 对接只改这里
    ├── main_window.py
    ├── pcl_chrome.py    ← 顶栏/侧栏/色板
    ├── widgets.py
    ├── fly_anim.py
    └── pages/
        ├── launch_page.py
        ├── version_page.py
        ├── catalog_page.py   ← Mod/整合包/数据包/资源包/光影
        ├── java_page.py
        ├── instance_page.py
        ├── settings_page.py
        └── tasks_page.py
```

`pages/mod_page.py`、`modpack_page.py`、`content_page.py`、`download_hub.py` 为旧页，主导航已改走 `catalog_page.py`，可忽略。
