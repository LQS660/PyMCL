<UserControl
    x:Class="PyMCL.Pages.InstancePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    SizeChanged="Page_SizeChanged">
    <Grid x:Name="PageRoot" Padding="28,20">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition FromVerticalOffset="20" IsStaggeringEnabled="True"/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Margin="0,0,0,14">
            <TextBlock Text="实例" FontSize="20" FontWeight="SemiBold"/>
            <TextBlock Text="每个实例相互隔离，放心折腾" Style="{ThemeResource CaptionTextBlockStyle}"/>
        </StackPanel>
        <ScrollViewer Grid.Row="1">
            <ItemsRepeater x:Name="GridView" Margin="0,0,8,0">
                <ItemsRepeater.Layout>
                    <UniformGridLayout MinItemWidth="220" MinItemHeight="138" MinColumnSpacing="12" MinRowSpacing="12"
                                       ItemsStretch="Fill" MaximumRowsOrColumns="6"/>
                </ItemsRepeater.Layout>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</UserControl>
