<UserControl
    x:Class="PyMCL.Pages.PlaytimePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="28,20" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="游玩时长" FontSize="20" FontWeight="SemiBold"/>
        <Button Grid.Row="1" Content="清除全部记录" HorizontalAlignment="Left" Click="Clear_Click"/>
        <ScrollViewer Grid.Row="2">
            <StackPanel x:Name="ListHost" Spacing="10"/>
        </ScrollViewer>
        <TextBlock x:Name="Empty" Grid.Row="2" Text="还没有游玩记录&#x0a;启动游戏后会自动记录"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   TextAlignment="Center" Opacity="0.6" Visibility="Collapsed"/>
    </Grid>
</UserControl>
