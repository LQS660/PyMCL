# PyMCL 新手教程 —— 从零开始，一步一步来

这份教程写给第一次接触 Python 和 Minecraft 启动器的朋友。
你不需要懂编程，只要跟着做就行。每完成一步，打个勾 ✓。

---

## 第 0 步：先搞清楚你要做什么

| 你的目标 | 要看哪几节 |
| --- | --- |
| 只是想玩游戏 | 第 1 ~ 5 步（用 Python 直接运行） |
| 想要一个双击就能打开的 exe | 第 1 ~ 6 步（最后打包成 exe） |

> 打包成 exe 需要一台装了 Python 的电脑。打包完成后，**用 exe 的那台电脑不需要装 Python**。

---

## 第 1 步：安装 Python

1. 打开浏览器，访问：**https://www.python.org/downloads/**
2. 点页面中间的黄色大按钮 **"Download Python 3.x.x"**（版本号无所谓，3.9 以上都行）
3. 下载完成后，双击运行安装程序
4. ⚠️ **最重要的一步**：安装窗口最下面有一个勾选框
   **"Add Python to PATH"（把 Python 加入 PATH）——一定要勾上它！**
   （很多新手就是卡在这一步）
5. 点 **"Install Now"**，等进度条走完，点 Close 关闭

## 第 2 步：检查 Python 装好没有

1. 按键盘 **Win 键 + R**（Win 键就是键盘左下角带窗户图标的键）
2. 弹出的小窗口里输入 `cmd`，按回车 —— 出现一个黑色窗口（这叫命令行）
3. 在黑色窗口里输入这一行，然后按回车：

   ```
   python --version
   ```

4. 如果显示 `Python 3.12.4` 之类的内容 → **成功了 ✓**
5. 如果显示"不是内部或外部命令" → 说明第 1 步的勾没打上，重新装一遍 Python，记得勾选

## 第 3 步：打开项目文件夹

你手上应该有一个文件夹（例如 `D:\新建文件夹 (5)`），里面有这些文件：

```
main.py           ← 程序入口
gui.py            ← 图形界面
build_exe.bat     ← 打包成 exe 的脚本（第 6 步用）
requirements.txt  ← 依赖清单
selftest.py       ← 自检脚本
mclauncher\       ← 程序核心（一个文件夹，别动它）
```

操作：打开这个文件夹，在**文件夹顶部的地址栏**里直接输入 `cmd` 然后按回车
—— 黑色窗口会直接定位到这个文件夹（这是个小技巧）。

## 第 4 步：安装运行需要的"零件"（依赖）

1. 在刚才打开的黑色窗口里输入：

   ```
   pip install -r requirements.txt
   ```

2. 按回车，等它下载完（一般十几秒）
3. 最后一行出现 `Successfully installed requests-2.xx.x` → **成功了 ✓**
   （如果很慢或者报红字，多半是网络问题，重试一次即可）

## 第 5 步：先运行起来试试

1. 在黑色窗口里输入：

   ```
   python selftest.py
   ```

   看到一堆 `[OK]` 就说明环境没问题 ✓

2. 再输入：

   ```
   python main.py
   ```

   按回车 —— **启动器窗口会弹出来**！

3. 第一次玩游戏的操作：
   - 点 **"版本"** 页 → 双击列表里的 `1.21.4`（或任意版本）→ 等待下载完成
     （第一次要下载游戏本体，几百 MB，耐心等）
   - 点 **"启动"** 页 → 账号选"离线模式"，用户名随便填一个英文名（如 Steve）
   - 点 **🚀 启动游戏** → 游戏窗口出现，搞定！✓

   命令行版本（可选）：

   ```
   python main.py install 1.21.4        ← 下载版本
   python main.py launch 1.21.4 -u Steve   ← 启动游戏
   ```

## 第 6 步：打包成双击即用的 exe（可选）

1. 回到文件夹，**双击 `build_exe.bat`**
2. 黑色窗口会自己干活（窗口里是英文提示，因为中文在命令行里容易乱码，属正常现象），第一次会先自动安装打包工具，然后开始打包，
   一共几分钟，中途别关窗口
3. 看到 **"[4/4] Done!"** 并自动弹出 `dist` 文件夹 → **成功了 ✓**
4. `dist` 文件夹里有两个文件：
   - **`PyMCL.exe`** ← 就是它！双击直接打开启动器（推荐用这个）
   - `PyMCL-CLI.exe` ← 命令行版本，一般用不到
5. 把 `PyMCL.exe` 复制到桌面、U 盘、任何地方都能直接用；
   游戏数据（配置、存档、下载的 Java）会保存在 exe 旁边

---

## 常见问题（卡住了看这里）

| 现象 | 原因和解决办法 |
| --- | --- |
| `python` 不是内部或外部命令 | 第 1 步没勾选 Add to PATH，重装 Python 并勾选 |
| pip 下载很慢或失败 | 网络问题，重试；或换手机热点 |
| 双击 exe 没反应 | 看 exe 旁边的 `pymcl-error.log` 文件，把内容发给会编程的朋友或 AI |
| 杀毒软件报毒 | PyInstaller 打包的 exe 常见误报，添加信任即可 |
| 打包时报 `KeyError: 'CACHE'` | 新版 PyInstaller 与 Python 3.11 的兼容 bug；脚本已固定使用稳定版 6.10.0，重跑即可 |
| 版本下载到一半失败 | 网络问题，重新点安装，会接着上次的进度继续 |
| 游戏打不开 | 看启动器"启动"页底部的日志，红色/报错信息就是原因 |
| 想玩整合包 | "整合包"页输入关键词点搜索（如 optimization），双击安装 |

---

## 附加功能：下载模组（Mod）

启动器里有个 **"模组"** 页，可以像应用商店一样搜模组装模组：

1. 点 **"模组"** 页
2. 搜索框输入模组英文名（如 `sodium`、`jei`、`fabric-api`），点 **"搜索"**
3. 双击搜索结果 → 自动匹配你实例的 Minecraft 版本和加载器下载（它需要的依赖模组也会一起下载）
4. 下方"已安装模组"列表能看到装了什么，点"删除选中模组"可以卸载

另外还支持：
- **从链接安装**：粘贴 Modrinth / CurseForge 模组链接
- **导入本地 jar**：选择电脑上已经下载好的 `.jar` 模组文件

命令行版：`python main.py mods search sodium` → `python main.py mods install sodium`

---

就这么简单：**第 1~5 步 = 直接玩，第 6 步 = 变成双击即用的 exe。**
