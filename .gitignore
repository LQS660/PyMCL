# Local config and secrets
config.json
.env
.env.*
!.env.example
*.log
ai_chats.json

# Runtime / generated
node_modules/
instances/
.minecraft/
cache/
build/
dist/
/java/
.cursor/
feedback_hub/data/
device_id
feedback_history.json

# Android local/build
android/.gradle/
android/.kotlin/
android/build/
android/app/build/
android/local.properties
android/_ai_decode.txt
android/regression-out/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Promo toolchain (reinstall locally)
promo/tools/venv/
promo/tools/node_modules/
promo/tools/ffmpeg/
promo/tools/ffmpeg-extract/
promo/tools/ffmpeg-release-essentials.zip
promo/tools/package-lock.json
promo/raw/*
!promo/raw/.gitkeep
promo/out/*
!promo/out/.gitkeep

# OS
.DS_Store
Thumbs.db
.pytest_cache/
_*.txt
_*.json
!requirements.txt
会话-*.md
*.mp4

# WinUI / .NET build
winui3/**/bin/
winui3/**/obj/
*.user
*.suo

# 本地产物与敏感数据（勿推送）
accounts.json
/.workbuddy/
/archive/
/0*.zip
/0*.exe
pymcl-feedback-client-src.zip
wpf/**/bin/
wpf/**/obj/
/_shots/
# 离屏冒烟脚本每跑一次就重画一张，删了下一轮又回来；按产物对待
/_*.png

# 一次性排查脚本。写临时脚本请丢系统临时目录，别再往仓库根攒。
# 之前只挡了 _*_repro / _*_tmp 这几个窄写法，实际攒下来的近 80 个都是从别的
# 名字进来的，所以这里按「仓库根 + 下划线开头」一刀切。真要长期留的脚本请挪进
# scripts/ 并去掉下划线前缀。
/_*.py
/_*.log
/_*.txt
/_*.json

# app/ 的整树备份副本，i18n 改造时留的
/_app_backup_i18n/

# PYMCL_HOME 落在仓库根时生成的实例数据（assets/libraries/saves，数百 MB）
/default/

# WPF 前端的本地 UI 状态（侧栏宽度、主题、布局）
wpf-ui.json

# 离线皮肤：整个目录都是 skinserver.py 在 utils.ROOT 下现建的。
# yggdrasil_key.pem 是每台机器自己生成的 RSA 4096 私钥（丢了会自动重建），
# 绝不能进仓库。
/skins/

# authlib.py 运行时按需下载，不是源码
/authlib-injector.jar

# _make_source_zip.py / _pack_*.py 的产物
/*-src.zip
/PyMCL-Qt-src.zip

# 配置备份（config.json 本体已在文件开头忽略）
config.json.bak*

# 构建日志
/build_*.log
/build_qt_log.txt

# 运行时统计
/playtime.json

# C / C++ 侧的编译产物
/native/build/
/uihost/build/

# 大体积审计/冒烟产物与冒烟输出（2026-09-19 清理）
/_android_audit/
/_wpf_smoke/
/smoke_*.txt

# 整包旧备份与一次性提示词（2026-09-19 清理，git 历史可找回）
/_app_backup_i18n/
/WINUI3_AGENT_PROMPT.md

# 图标生成管线的中间产物（proc/raw，成品已内联进 icon_assets.py / LucideAssets.cs）
/_gen_icons/
/_appicon/
/PyMCL.ico
/PyMCL-icon-1024.png
# AI 上游 SSE 抓包
/_upstream_cap/
# native 构建前的旁路 DLL 备份
/native/_build_dll_backup/
# 迁移前的整包备份 zip 与预览大图
/_backup_*.zip
/_*_view.jpg
