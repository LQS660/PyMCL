# 接手续验记录 — 2026-09-17

## 边界

- 保持已有工作树，不清理或回滚其他修改。
- 测试后台运行；微软正版登录按要求跳过。
- **Android / WPF 与 Qt 完整行为对齐尚未完成。** 本记录不是全量验收证明。

## 本轮修改

### Android

- DocumentImport 私有临时目录暂存 SAF 文件，成功/异常均清理；回调命名改为 consumeFile。
- 六个新增测试验证文件名与字节保留、流关闭、非法名/扩展名拒绝、读取失败、导入失败及同名隔离。
- 内容库刷新采用取消旧任务、请求代次和目标快照校验；目录扫描及 Mods.list 均在 IO 上执行，避免旧结果覆盖新页面。
- SAF 导入记录启动选择器时的实例/类别/版本，不再在回调时随当前标签改变目的地。
- 非模组目录提示与实际导入目的地一致；启停模组后刷新内容库。
- 版本设置保存等异步结果成功才关闭，失败保留编辑页并展示错误；实例切换不复用旧设置草稿。

### WPF

- 冒烟白名单改为精确匹配；仅明确允许“链接安装”打开输入框并取消。
- 对话框不再回退点击任意首按钮；无明确取消按钮时调用对话层取消关闭回调。
- 系统文件选择器模拟取消与实际 WPF 弹窗分别计数，10 个弹窗门槛只认后者。
- 默认离屏、隐藏任务栏、ShowActivated=false、WS_EX_NOACTIVATE；对话层不主动 Focus。
- 冒烟禁用动画，避免中间透明帧取证。
- PageBase.Run 跟踪异步操作、记录异常；按钮检查等操作及对话层结束。其他 async void 处理器尚未全面接入跟踪。

## 当前实跑证据

### Android JVM 全树检查：通过，但不等于 APK

- 目录：`_android_audit/20260917-231210/`
- 102 个主源码文件、31 个测试源码文件；未排除源码或测试；JUnit **653 tests / 0 failures**。
- 使用缓存 AndroidX 和已编译 FCL 类，包括历史 FCLCore 快照；并非从源码重建全部上游。
- 第一次复用旧 harness 失败是 transforms-4 缓存路径失效；重新发现当前 8.13 transforms 后才通过。失败日志保留。
- 脚本及 classpath 固化于该目录 run.py / main.args / test.args，是本机复验材料，不是便携构建系统。

### Android 标准 Gradle：失败

- 日志：`_android_audit/gradle-20260917-230844/build.log`
- Gradle 8.13、SDK 明确配置，offline / no-daemon；关闭 Kotlin 增量并使用 in-process 后，本次不再出现跨 C:/D: 根目录错误。
- `:app:processDebugResources` 仍报 AAPT2 8.13.2 link unexpected error。
- 未归因于杀毒软件、未关闭安全软件；APK、设备运行、SAF 界面回归仍未验收。

### WPF：构建通过，严格冒烟仍有失败

- dotnet build：0 错误 / 0 警告。
- 正常轮：`_wpf_smoke/run-20260917-231114/report.json`
  - 19 页面、46 按钮、16 个实际 WPF 弹窗；覆盖门槛已满足。
  - 数据包 / 资源包 / 光影页超过 9 秒加载预算；**exit 1，不伪报通过**。
  - 检查发现这些页面初载会等待在线搜索，尚需区分网络响应耗时和真实页面问题。
  - 前台归属采样 860 次（100ms 间隔），该 WPF 进程占前台 0 次。不能证明采样间隙无瞬态。
- 负向轮：`_wpf_smoke/negative-p9000-c0-20260917-231241/report.json`
  - 0 点击 / 0 弹窗，覆盖门槛正确报错，exit 1；另有光影页加载超时。
  - 前台归属采样 402 次，该进程占前台 0 次。
- 较早一次 foreground_changes 仅记录全系统前台改变，不能据此判断本应用抢焦点；以上按 PID 归属的新采样才用于结论。

## 尚未交付 / 下轮优先级

1. 诊断 AAPT2 资源链接失败，获取完整 Gradle/资源工具堆栈，再尝试打包和设备验证。
2. WPF 在线目录加载超时：可取消搜索、超时与错误显示、异步任务全覆盖，不能单靠加大门槛。
3. Android 内容库：导出、整合包真正安装流程与 Qt 对照；选择器跨配置变更的上下文保存、删除确认框上下文固定；保存失败/切换实例/UI 并发行为要设备验证。
4. Qt 逐行为验收表（已有旧审计只作基线）：侧栏布局、实例、版本、账号（非微软）、下载六类、联机、服务器、任务、AI、设置和错误路径逐项列运行证据。
5. 当前未重跑 Python 全套；先前 319 passed / 1 skipped 属于前轮证据。
