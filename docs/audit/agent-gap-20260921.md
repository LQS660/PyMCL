# 全仓库「名不副实」审计 · 2026-09-21

> 基线：分支 `fix-8items` @ `eb83cd3`（阶段 0 已把 21 个未提交改动按主题拆成 8 个 commit，工作区干净）。
> 实测基线：`C:/Python312/python.exe -m pytest tests -q --timeout=300`（QT_QPA_PLATFORM=offscreen）→ **491 passed / 1 skipped / 0 failed** + 90 subtests；skip 仅 `test_root_checks.py:150` 的 `_layout_smoke`（需 `PYMCL_SLOW_CHECKS=1`）。
> 方法：AI 内核（`mclauncher/ai/` 19 模块 4249 行）逐行人工过；其余按四类口径全仓库扫描 + 现成探针脚本复跑 + 关键行实机复现（复现编号 V1–V5 见 B 表）。
> **行数统计：A: 11 · B: 15 · C: 6 · D: 14**（合计 46 行；每行判定只能是「真实缺陷 / 设计如此（附理由）/ 平台不适用」，本次无「无法判定」条目）。
> 机器核对口径：`grep -cE '[^ ]+\.(py|cs|kt|ts):[0-9]+'` 命中 34，对应 A/B/C 三表 32 行（每行 1–3 个代码引用）；D 表声称来源均为文档（.md）与命令输出，本身没有代码行号可引，不参与该计数。

---

## A 表 · UI 接了但后端没实现（11 行）

| # | 调用点 | RPC / 方法 / 信号名 | 后端实际实现位置 | 说明 | 判定 |
|---|---|---|---|---|---|
| A1 | wpf32/PyMCL.Wpf/Pages/AccountPage.cs:156 | `delete_account` | 不存在（两后端只有 `remove_account`：bridge/api.py:1235、app/backend.py:1456） | 删除账号先走 `remove_account`，失败兜底再调 `delete_account`——该 RPC 桥端不存在，`bridge/server.py:232` 必回 -32601，兜底必再抛 | 真实缺陷 |
| A2 | wpf32/PyMCL.Wpf/Pages/JavaPage.cs:188 | `open_java_folder` | 不存在 | 探测失败时本地 `explorer.exe` 兜底（187 行注释明说） | 设计如此（有兜底，功能不缺） |
| A3 | wpf32/PyMCL.Wpf/Pages/InstancePage.cs:73 | `set_default_instance` | 不存在 | 注释明说缺了走 `update_settings{default_instance}` 补丁兜底 | 设计如此（同上） |
| A4 | wpf32/PyMCL.Wpf/Pages/InstancePage.cs:130 | `set_default_instance` | 不存在 | 右键菜单同 A3 兜底 | 设计如此 |
| A5 | wpf32/PyMCL.Wpf/Pages/VersionPage.cs:230 | `set_default_instance` | 不存在 | 同 A3 兜底 | 设计如此 |
| A6 | android/app/src/main/java/com/pymcl/mobile/data/AiTools.kt:225 | 10 个写入类 AI 工具（install_game/install_mod/install_modpack/create_instance/delete_instance/delete_mod/disable_mod/enable_mod/launch_game/write_mod_config） | 不存在（`run=null`，`WriteGate.DENY`：AiTools.kt:57、AiAgent.kt:46） | schema 照常声明给模型，调用后回「写入类工具…尚未开放执行」（AiTools.kt:343）；注释明说本轮不接执行 | 设计如此（登记在案：Android 写路径本轮范围外，缺写确认 UI，外部工作树已有 AiWriteApproval.kt 未入库） |
| A7 | android/app/src/main/java/com/pymcl/mobile/data/AiTools.kt:129 | `ask_user` | 桌面存在（mclauncher/ai/tools.py:64）；Android 全仓零实现 | 桌面把 ask_user 定为强交互约定（mclauncher/ai/prompt.py:55），手机端整条链路缺席且无占位说明 | 真实缺陷（平台差异未登记；只登记不修，见门禁报告） |
| A8 | android/app/src/main/java/com/pymcl/mobile/data/AiTools.kt:129 | 桌面 34 工具 vs Android 19 | Android 缺 17 个桌面工具（search_content/search_worlds/install_shader/get_latest_log/get_crash_report/inspect_mod/list_mod_configs/read_mod_config/read_artifact 等） | docs/audit/alignment-checklist-2026-09-18.md 声称安卓 100% 对齐，但口径只含 BackendAPI 方法层，不含 AI 工具层 | 真实缺陷（对齐声称失实；本轮只登记） |
| A9 | 无调用点（反向） | bridge `list_tasks` | bridge/api.py:2682（实现完整） | docstring 自述「前端重连后对账运行中任务」，全仓库无任何前端调用 | 真实缺陷（后端就绪、前端未接） |
| A10 | 无调用点（反向） | bridge `ai_steer` | bridge/api.py:2805（实现完整） | 「跑动中插话」能力，WPF/eziapp 均未接（Qt 进程内另有通道） | 真实缺陷（后端就绪、UI 未接） |
| A11 | wpf32/PyMCL.Wpf/MainWindow.xaml.cs:230 | `update_settings` | bridge/api.py:994（存在） | `_ = TryCallAsync(...)` 发后不理，失败无提示 | 设计如此（Try 前缀即有意吞错，主题切换可接受） |

非缺陷排查记录：Qt 27 个页面的 backend 调用逐一比对仅 1 处误报（局部变量）；两后端 400+ 公开方法 AST 检测无空体桩；eziapp 153 个 RPC 名与 WPF 主版全部命中桥方法表；winui3 的 72 个 RPC 名全命中。

## B 表 · 后端实现存在但是坏的（15 行）

| # | 位置 | 问题描述 | 复现步骤 | 实际结果 | 期望结果 | 影响面 | 判定 |
|---|---|---|---|---|---|---|---|
| B1 | main.py:554 | Qt 导入失败回退 `from gui import PyMCLApp`，但全仓库无 gui 模块 | 无 PySide6 环境跑 `python main.py` | ImportError 后再抛 ModuleNotFoundError，被外层捕获「图形界面启动失败」，回退分支永不可达【V1 实测：find_spec("gui")=None】 | 回退可达或删除死分支 | CLI-only 分发/缺依赖环境 | 真实缺陷 |
| B2 | mclauncher/installer.py:613 | `_run_processors` 调 `utils.run_process` 未传 timeout（默认 None 无限阻塞），同文件 1133/1168 两处都带 timeout=1800 | 安装 version.json 自带 processors 的版本，某处理器卡死 | 安装任务永久挂起，cancel 标志在 `proc.wait` 期间不被轮询，无法取消 | 与另两处一致加超时 | bridge/api.py 与 app/backend.py 安装任务链 | 真实缺陷 |
| B3 | mclauncher/launch_flow.py:79 | `run_hook` 用 `subprocess.run(shell=True)` 无 timeout、不查取消标志；prepare 默认 wait=True | 版本配置了阻塞式启动前脚本 | 启动 worker 永久卡住；此刻无游戏进程，cancel_task 无的放矢 | 加超时 + 取消轮询 | 两条启动链（app/backend.py:2381、bridge/api.py:2043） | 真实缺陷 |
| B4 | ai_gateway/server.py:172 | `min(float(body.get("temperature") or 0.3), 0.8)` 等无 ValueError 防护，do_POST 无 except | 向 /pymcl/chat POST 非数字 temperature | 处理线程抛未捕获异常，连接直接关闭、无任何 HTTP 响应【V4 实测：float("hot") 必抛】 | 返回 400 可读错误 | 全部网关调用方（第三方脚本必炸） | 真实缺陷 |
| B5 | ai_gateway/server.py:227 | 转发 SSE 时 `except Exception: pass` 吞掉上游中断；客户端 `_assemble_stream` 在 EOF 且已有 delta 时 yield done（mclauncher/ai/client.py:370） | 上游流中途断连 | 用户看到半截回答且状态为「完成」，不触发重试/压缩；client 注释明知「只有 [DONE] 才是流结束」 | EOF 无 [DONE] 产出 error 事件 | AI 对话全链路 | 真实缺陷 |
| B6 | mclauncher/installer.py:1196 | `_install_forge_cli` 用 `installed_ids()` 字母序 `found[-1]` 当新装版本号 | 实例已有 47.3.11 与 47.10.0，再走 CLI 回退装新版 | sorted 后 found[-1]=47.3.11，实例默认版本指向错误【V5 实测】 | 用刚 copytree 回来的目录名 | CLI 回退安装后的版本指向 | 真实缺陷 |
| B7 | mclauncher/playtime.py:83 | sessions 只留 500 条，clear_playtime 清单个版本时按剩余 sessions 全量重算 total | 累计超 500 次会话后清除某版本时长 | total 缩水（被截断的老会话蒸发） | 从原 total 减目标版本累计 | playtime_page 统计 | 真实缺陷 |
| B8 | mclauncher/instances.py:31 | `_STANDARD_DIRS` 四个目录整组重复两遍（18 项去重后 14） | `Instance.create()` | 目录 ensure 两遍（幂等无害） | 去重 | 无运行时危害，代码卫生 | 真实缺陷（低危死代码） |
| B9 | ai_gateway/server.py:110 | `_err` 内 `payload, _ = None, None` 赋值后从未使用 | 走 _err 分支 | 无影响 | 删除 | 无 | 真实缺陷（低危死代码） |
| B10 | _bridge_api_backup.py:1 | 仓库根存放 bridge/api.py 的陈旧整份备份（2822 行，落后现版 400+ 行） | 被打包 spec 或工具按 *.py 误收 | 死文件随源码分发、误导审计 | 移出源码根 | 打包体积/审计干扰 | 真实缺陷（低危） |
| B11 | mclauncher/ai/tools.py:776 | `parse_args`/`run_tool` 解析失败静默返回 `{}`，工具以空参数执行 | 模型吐出截断 JSON 参数 | 工具静默以空参跑，如 install_mod 无 name 直接失败一轮 | 返回结构化错误（含字段名）给模型自纠 | 全部 34 个工具（批次 5.1 靶点） | 真实缺陷 |
| B12 | mclauncher/ai/tools.py:787 | `run_tool` 把原始异常串 `f"工具失败: {exc}"` 直接喂给模型，堆栈不落结构化错误码 | 任一工具抛异常 | 模型收到含路径/内部细节的原始串 | 错误码 + 用户可读文案，堆栈只进 trace | 全部工具（批次 5.2 靶点） | 真实缺陷 |
| B13 | mclauncher/ai/builtin.py:13 | XOR 混淆的共享上游令牌内置进分发包；`DEFAULT_GATEWAY_URL=""`（mclauncher/ai/defaults.py:7）使默认走内置令牌通道（mclauncher/ai/client.py:151） | 对产物做字符串扫描 | 可还原出可用上游令牌（混淆不是加密） | 分发包不含可用令牌；未配置网关时明确提示（批次 4.1 靶点） | 商业/公益上游令牌安全 | 真实缺陷 |
| B14 | mclauncher/ai/compact.py:102 | `context_window=200_000` 硬编码 + tokens.py:5 自认「未实测」；压缩阈值由它派生 | 用真实窗口小于 200k 的模型长对话 | 阈值失真，autocompact 触发过晚 → 上游 400 | 实测结论或保守值 + 明确待测标记（批次 2.1 靶点） | 上下文压缩正确性 | 真实缺陷 |
| B15 | app/pages/ai_page.py:1056 | 「撤回」tooltip 写「撤回最近一轮对话和它的工具操作痕迹」，实现只截断聊天记录（`_rewind`），磁盘写入不还原；全工程除 `write_config` 的 .bak 外无任何回滚通道 | 写配置→点撤回→查磁盘 | 磁盘文件仍是改后状态，用户无从得知 | 语义无歧义：要么真回滚磁盘（需检查点机制），要么文案只说回滚对话（批次 1.1/1.3 靶点） | 用户对 AI 写操作的可逆性预期 | 真实缺陷 |

## C 表 · 测试假绿（6 行）

全库核查：47 个测试文件逐一浏览 + AST 扫描，未发现 patch 掉被测函数本身的用例（全部 patch 均为协作者/下游 I/O）；无断言写反实例。

| # | 位置 | 测试 | 假绿类型 | 证据 | 危害 | 判定 |
|---|---|---|---|---|---|---|
| C1 | tests/test_ai_stop_reason.py:172 | TraceTests.test_trace_never_raises | 恒真（只断言不抛，且被调方 trace.py:75 自吞一切异常） | 函数体无 assert，仅调用 record | record 若提前 return/丢字段照样绿 | 设计如此（契约即「永不抛」，但属真·恒真测试） |
| C2 | tests/test_search_ranking.py:86 | test_identical_hits_do_not_raise_on_tie_break | 只断言不抛（裸调用零断言） | `rank_hits(same, "t", "mod")` 无断言 | 返回 []/丢项/乱序仍绿 | 设计如此（防当年 TypeError 回归，建议补集合断言） |
| C3 | tests/test_offline_skin.py:229 | test_both_backends_expose_the_same_methods | 弱断言（只测方法存在性） | `assertTrue(callable(getattr(cls, ...)))` | 签名/行为漂移不拦 | 设计如此（parity 门禁；行为由同文件 round_trip 用例兜底） |
| C4 | tests/test_layout_rpc.py:181 | test_qt_backend_exposes_same_layout_methods | 弱断言（同上） | `assertTrue(callable(...))` | 参数与语义漂移不报 | 设计如此（parity 门禁） |
| C5 | tests/test_force_launch.py:105 | test_launch_game_force_signature_mirrored | 弱断言（静态签名检查） | `inspect.signature` 查参数存在 | 不执行行为（行为由同文件另两用例真覆盖） | 设计如此（双端镜像门禁） |
| C6 | tests/test_audit_fixes.py:228 | test_search_content_dispatches_by_kind | 局部弱断言 | `self.assertTrue(out)` 冗余 | 主断言 assertEqual(calls[...]) 有效 | 设计如此（非假绿，冗余断言） |

skip 全列表（pytest -rs 实测）：运行时仅 1 条 `tests/test_root_checks.py:150`（`_layout_smoke` 慢门禁，`PYMCL_SLOW_CHECKS=1` 才跑，条件门禁非僵尸）。静态条件门禁 5 类：test_nav_parity.py:193（缺 node 整类跳）、test_nav_parity.py:202 / test_wpf_feedback.py:158 / test_wpf_i18n.py:320（需预编译 WPF exe）、test_wallpaper_frames.py:62（无 QtMultimedia 跳）。风险：CI 不带 node / 不预编译 WPF 时这些真值表用例无声消失——代码内均有理由文案，属环境门禁，非长期挂起。

## D 表 · 无门禁链路（14 行）

CI 实况（.github/workflows/checks.yml，唯一工作流）：仅 `check_qt_parent.py` + pytest（offscreen，py3.12）。未覆盖：eziapp tsc、WPF/winui3 dotnet 编译、native 桥、android、PyInstaller 打包、selftest、ai_gateway 服务。

| # | 链路 | CI 覆盖 | 声称来源 | 可跑性结论 | 实际命令与输出摘要 | 判定 |
|---|---|---|---|---|---|---|
| D1 | Qt 桌面 | 是 | README.md:19 | 能跑（测试级） | pytest 491 passed/1 skipped；check_qt_parent 退码 0（18 类 246 构造点全过） | 设计如此 |
| D2 | CLI（install/launch/mods） | 部分（核心有测试，端到端无） | README.md:75 | 能跑（未逐条实跑避免真实下载） | selftest.py 退码 0：915 版本清单、Modrinth 可用、Java 17 | 设计如此 |
| D3 | AI 助手（Qt） | 是（test_ai_* 8 文件） | README.md:117 | 能跑（测试级） | 同 D1，全绿 | 设计如此 |
| D4 | eziapp/Web | 否 | docs/STATUS.md:17 | 能跑 | `npx tsc --noEmit` 退码 0；nav_model.test.ts 与 layout_geom.fuzz.ts（59959 断言）全过 | 设计如此，无 CI 门禁 |
| D5 | WPF 编译 | 否（pytest 仅文案级） | docs/STATUS.md:18 | 能跑（编译级） | `dotnet build PyMCL.Wpf/PyMCL.Wpf.csproj -c Release` → 0 警告 0 错误 3.68s | 设计如此，无 CI 门禁 |
| D6 | 桌面发行包（Desktop\\PyMCL-Qt / PyMCL-WPF） | 不适用 | docs/STATUS.md:1 声称「已更新到桌面」 | **跑不起来（不存在）** | `ls` 两路径均 No such file or directory | 真实缺陷（文档失实/环境已变，见「不符之处」#1） |
| D7 | winui3 | 否 | docs/STATUS.md:19 | 能跑 | `dotnet build PyMCL.WinUI.sln -c Release` → 0 警告 0 错误 13.22s | 设计如此，无 CI 门禁 |
| D8 | native C 桥 | 否 | docs/STATUS.md:20 | 能跑（冒烟级） | 产物 pymcl-bridge.exe 在；无参运行打印用法退码 2（符合声称）；完整 RPC 会话双方均未验证 | 设计如此（STATUS 已自认），完整链路无门禁 |
| D9 | ai_gateway 服务 | 否 | README.md:135 | 未实测（避免长驻进程），文件齐备 | ai_gateway/ 含 server.py、.env.example、README | 设计如此，服务本身无门禁（B4 另计） |
| D10 | Android | 否 | README.md:24 声称「可编译浏览 UI」 | **跑不起来（按文档证据）** | docs/audit/continuation-2026-09-17.md:43 记录 AAPT2 link 阻塞未解决；本次未真跑 gradle | 真实缺陷（README 声称与审计记录冲突，见「不符之处」#2） |
| D11 | PyInstaller 打包产物 | 否 | README.md:34、新手教程.md:101 | 跑不起来（当前无产物；未重跑打包） | dist/ 不存在；pyinstaller 6.10.0 已装；build_exe.log 尾部「completed successfully」证明历史曾成功 | 设计如此（产物不进版本控制），但打包链路无门禁未复验 |
| D12 | selftest.py 自检 | 否 | docs/STATUS.md:16 | 能跑（依赖网络） | 退码 0 全 OK | 设计如此，无 CI 门禁 |
| D13 | 对接层（对接说明.md） | 部分 | 对接说明.md:5/244/258 | 跑起来但结果不可信（文档与代码不符） | backend 实为 mclauncher 真实现非「内存模拟」；run.bat 不存在（实际 start.bat）；pymcl_launcher/ 目录不存在 | 真实缺陷（文档三处过期，见「不符之处」#3） |
| D14 | 新手教程文件清单 | 不适用 | 新手教程.md:48 | 跑不起来（gui.py 不存在） | `ls gui.py` → No such file | 真实缺陷（见「不符之处」#4） |

## 与 docs/STATUS.md / docs/audit/ 既有结论不符之处

1. **桌面发行包**：docs/STATUS.md:1 声称「两份发行包已更新到桌面（Desktop\\PyMCL-Qt / PyMCL-WPF）」——实测两目录均不存在。以本次实测为准：发行包当前不可用，重打包是后续事项（本轮范围外）。
2. **Android 可编译**：README.md:24 声称 Android「可编译浏览 UI」；docs/audit/continuation-2026-09-17.md:43 记录 `AAPT2 link unexpected error` 阻塞未解决且「APK、设备运行、SAF 界面回归仍未验收」。两文档互相冲突，以较新审计为准：Android 构建链路当前跑不起来。
3. **对接说明.md**：声称 backend 为「内存模拟」，实测 app/backend.py 已接 mclauncher 真实现；声称入口 run.bat（实际 start.bat）；声称目录 pymcl_launcher/（不存在）。整份文档过期。
4. **新手教程.md:48**：文件清单里的 `gui.py` 不存在（main.py 的回退分支也因此永不可达，见 B1）。
5. **测试基线数字**：docs/STATUS.md 记 476 passed @ 60cc011；本次实测 491 passed / 1 skipped @ eb83cd3（阶段 0 提交的改动含 2 个新测试文件共 +15 用例）。历史数字过期，以本次为准。
6. **对齐声称口径**：docs/audit/alignment-checklist-2026-09-18.md 声称安卓 175/175=100% 对齐，但口径只含 BackendAPI 方法层，不含 AI 工具层（实际桌面 34 vs Android 19，见 A7/A8）。
7. **bridge-protocol-and-gap.md / feature-parity-matrix.md**：STATUS 已自注其 §2c bug 清单与 §E P0 过期，本次复核维持该结论；其中「桥多 15 个 AI 方法」的差值仍成立（Qt 走进程内，桥端为 WPF/ezi 补的 AI 面）。
8. **本次未发现「声称已修但实测仍坏」的代码级情形**：上轮红灯（test_bridge_i18n 缺词条、test_bridge_parity note 键、test_wpf_i18n 缺权限文案）实测均已绿。文档级失实共 4 处（#1/#3/#4 与对齐口径 #6）。

## 需要需求方裁决

本次无「无法判定」条目——四张表每行均已给出明确判定。范围外仅登记不修的项：A6/A7/A8（Android）、D10；其余缺陷的修复批次归属已在各行标注对应批次号（1.1/1.3/2.1/4.1/5.1/5.2）。
